
name: 'olist_db_proj'
version: '1.0.0'
profile: 'olist_db_proj'


model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         
  - "target"
  - "dbt_packages"

models:
  olist_db_proj:
    staging:
      +materialized: view
      +schema: staging 
    
    analytics:
      +materialized: table
      +schema: analytics

    marts:
      +materialized: table 
      +schema: reporting

vars:
  latest_date: '2018-10-17'
  dim_date_start: '2016-09-03'